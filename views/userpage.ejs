<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyPortfolio</title>
    <link rel="stylesheet" href="/userpage.css">
</head>
<body>
    <header>
        <nav>
            <button id="addText">Add Text Field</button>
            <button id="addImage">Add Image Field</button>
            <button id="Savebtn">Save</button>
            <button id="logoutbtn">Logout</button>
            <input type="color" id="bgcolor">
        </nav>
    </header>
    <div class="tooltip" id="tooltip" style="display: none;"></div>
    <div class="container">
        <label>Your Portfolio is Live at this URL, Just copy and Share.</label>
        <div class="for_share">
        <span class="copy-text" id="textToCopy">https://example.com/your-portfolio</span>
        <button class="copy-button" onclick="copyText()">Copy</button>
        </div>
        <br>
        <label>It is the workspace for Your Portfolio</label>
    </div>

    <main id="editor" style="background-color: <%= css === 'No' ? '#ffffff' : css %>;">
        <% if (html === 'No') { %>
            <!-- Dynamic fields will appear here -->
        <% } else { %>
            <%- html %> 
        <% } %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/interactjs@1.10.11/dist/interact.min.js"></script>
    <script src="/userpage.js"></script>
</body>
</html>
